package com.example.tourism_app;

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // Uses Flutter v2 embedding by default - no additional code needed
} 